# 黎安 · 校园漫游

可在浏览器运行的校园外部漫游原型。当前场景是**电子科技大学海南国际学院一期的局部灰模**，支持鸟瞰、第一人称、第三人称、行走、跑步、跳跃、建筑碰撞、小地图和资料搜索。其他学校目前完成资料检索，尚未加入场景。

## 打开试玩

本次会话预览：http://127.0.0.1:4173/ 。这是当前电脑的本地地址，不是公网分享链接；预览进程停止后需重新启动。

已包含编译好的 `dist/`，无需重新安装依赖即可运行。macOS 上运行 `启动漫游.command` 后，打开 http://127.0.0.1:5175/ 。需要本机有 Python 3。也可在项目目录执行：

```sh
python3 -m http.server 5175 --bind 127.0.0.1 --directory dist
```

不要通过双击 `dist/index.html` 的 file:// 地址运行：场景数据和物理 WASM 需要 HTTP 服务。

## 操作

- 鸟瞰：拖动旋转，滚轮缩放。
- 漫游：WASD/方向键移动；鼠标转向；Shift 跑步；Space 跳跃；V 切换一/三人称。
- Esc 释放鼠标，点击场景继续；“回到入口”可重置位置。
- 触屏：方向按钮移动，拖动场景转向，跳跃按钮起跳。
- “?” 中提供建模依据和重置入口；资料库可按学校/设施搜索。
- 建模依据窗口可切换实建参考底图。该底图用于平面核对，不能作为三维地形表面。

## 数据依据和精度

来源：[管理局电子科大一期规划核实公告](https://www.study-hn.cn/NewsDetail/92ad197add3b490fbd6a7e64325fe44f/MjAyNS0wNy0wMQ==/1?nav=%5B%5D)及[官方原始PDF](https://wap.study-hn.cn/upload/file/2025/08/04/a8a440f163b84513bc2628a0b16455a7.pdf)。采用第4页右侧“实际建设位置图”，不是左侧报建图。

- 8个界址点进行仿射校准，场景1单位=1米；原图E/N坐标转为以E395320、N2035100为原点的局部坐标。X向东、Z向北。
- 控制点拟合最大残差约0.316米，**仅衡量人工取点拟合一致性，不代表模型测绘精度**。轮廓、曲线和建筑细部均有人工简化。
- 建筑分块人工描绘，层数来自图面；层高暂统一估算为3.9米，不是实测高度。没有精确立面、门窗、室内或可通行建筑入口。
- 地形用8个图面高程读值插值，竖向以15.5米为场景零点；不是DEM，缺少坡折、挡墙、台阶和完整竖向控制。
- 道路宽度、铺装、中庭、树木和导览标记为示意；树木位置不代表实际树位。
- 没有把其他学校的效果图自动推断成真实模型。图纸“规划/实建”及不同日期要分开。

`public/campus.json` 保存可替换的边界、建筑、道路和高程点；`tools/build_campus_data.py` 保存人工取点、坐标校准和生成步骤，需要 Python/numpy，可在后续修改。

## 资料库

`public/documents.json` 有55条入口：40个新增附件 + 此前11份资料 + 4条尚未下载的官方网盘线索。新增40文件中有2组重复哈希，不能按40个独立建筑理解。

完整研究清单在同级“其他学校与校园设施资料/学校与校园设施资料清单.md”，含所有原件的来源、建模用途、版本冲突与待补项。网页仅保留源链接，不把333MB规划原件装进游戏首屏。

优先扩建顺序：北邮40页实建资料 → 综合体育中心/会堂/大学生活动中心/生活一区 → 中传及东北林业规划组团 → 其余待核项目。公共教学服务中心等网盘附件因浏览器安全策略无法打开，当前未核验。

## 开发与发布

技术：Babylon.js 9.26.0、Havok 1.3.14、TypeScript、Vite；版本固定在package.json与package-lock.json。

```sh
npm ci
npm run dev
npm run build
npm run preview -- --port 4173
```

Node版本要求以安装的Vite包声明为准；本次使用本机Node完成构建。开发诊断接口只在开发模式存在，生产构建移除。场景控制、数据和HUD集中在 `src/main.ts`，样式在 `src/style.css`。

`dist/` 可部署到支持HTTPS和WASM的静态网站服务。当前按域名根路径构建；若部署到子目录，先配置Vite的base并重新构建。还未部署公网域名，也没有账号、多人联网、任务系统、存档或完整校园。

## 本次验证

- TypeScript检查及Vite生产构建。
- Chrome中实际加载场景，无页面运行时错误；55条资料入口，搜索“北京邮电”得到2条对应资料。
- WASD行走1.5秒约5.46米；跳跃后高度上升约0.76米，随后回到地面附近。
- 持续向已知建筑外墙前进6秒，角色被挡住并沿墙移动，采样点未进入实体轮廓。
- 打开资料窗口后按W，角色位置保持不变。
- 375×812和812×375视口检查，无横向溢出；尚未在真实手机硬件上测性能。
- 桌面测试约60FPS仅为本机表现；宽度变化时也会短暂降低，不代表所有设备帧率。

验证采样保存在“验证记录/”。初版主要性能成本是约6.7MB未压缩引擎主包与2.1MB物理WASM；后续扩图需分区加载、材质与几何合并、远近细节层级和移动端画质档位。
